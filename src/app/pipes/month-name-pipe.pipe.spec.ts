import { MonthNamePipePipe } from './month-name-pipe.pipe';

describe('MonthNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MonthNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
